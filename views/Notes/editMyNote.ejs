<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Edit — <%= note.title %></title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
  <div class="container py-4">
    <div class="d-flex justify-content-between align-items-center mb-3">
      <h1>Edit Note</h1>
      <a href="/mynotes" class="btn btn-outline-primary">Back</a>
    </div>

    <div class="card">
      <div class="card-body">
        <form action="/mynotes/edit/<%= note._id %>?_method=PUT" method="POST">
          <div class="mb-2">
            <input required name="title" value="<%= note.title %>" class="form-control" placeholder="Title">
          </div>
          <div class="mb-2">
            <textarea required name="content" rows="8" class="form-control" placeholder="Write your note..."><%= note.content %></textarea>
          </div>
          <div class="text-end">
            <button class="btn btn-primary">Save Changes</button>
          </div>
        </form>

        <form class="mt-2" action="/mynotes/delete/<%= note._id %>" method="POST" onsubmit="return confirm('Delete this note?');">
          <button class="btn btn-danger">Delete Note</button>
        </form>
      </div>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>