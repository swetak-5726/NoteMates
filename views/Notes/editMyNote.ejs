<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Edit — <%= note.title %></title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/mynotes.css">
</head>
 <body>
    <%- include("../partials/navbar") %>
    <%- include("../partials/messages") %>

<div class="container py-5">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <div style="width: 80px;"></div>
        <h1 class="page-title text-center flex-grow-1">Edit your Note</h1>
        <a href="/mynotes" class="btn btn-outline-light back-btn">
            <i class="fa-solid fa-arrow-left-long me-1"></i> Back
        </a>
    </div>

        <h3>Title - <%= note.title %></h3>
        <br>
    <div class="edit-card">
        <div class="edit-body">

            <form class="needs-validation" novalidate action="/mynotes/edit/<%= note._id %>?_method=PUT" method="POST">
                
                <label class="form-label mt-3">Content</label>
                <textarea required name="content" rows="8" 
                          class="form-control input-dark" 
                          placeholder="Write your note..."><%= note.content %></textarea>
                <div class="invalid-feedback">Content cannot be empty.</div>

                <div class="text-end mt-3">
                    <button class="btn btn-save">
                        <i class="fa-solid fa-floppy-disk me-1"></i> Save Changes
                    </button>
                </div>
              </form>

        </div>
    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>