<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>NoteMates</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link rel="stylesheet" href="/mynotes.css">

</head>
<body>

<%- include("../partials/navbar") %>
<%- include("../partials/messages") %>

<div class="container text-center mt-5">
    <h1 class="notes-title">My Notes</h1>
    <p class="notes-subtitle">Your personal collection — organized, accessible, and beautifully arranged.</p>
    <a href="/mynotes/createpage" class="btn btn-outline-light create-note">Create Note</a>
    <% if (notes.length === 0) { %>
        <p class="no-notes">You haven't created any notes yet.</p>
    <% } else { %>

    <div class="notes-grid">
        <% notes.forEach(note => { %>
            <div class="note-card">

                <h3 class="note-title">
                    <i class="fa-solid fa-book"></i> <%= note.title %>
                </h3>
                <small class="uploaded-on">
                    <i class="fa-regular fa-calendar"></i> 
                    <%= note.createdAt.toDateString() %>
                </small>

                <div class="note-actions">
                    <a href="/mynotes/view/<%= note._id %>" class="btn btn-outline-light">
                        <i class="fa-solid fa-eye"></i> View
                    </a>
                    <a href="/mynotes/edit/<%= note._id %>" class="btn btn-outline-light">
                        <i class="fa-solid fa-download"></i> Edit
                    </a>

                    <a href="/mynotes/delete/<%= note._id %>" class="btn btn-outline-light"
                      onclick="return confirm('Are you sure you want to delete this note?');">
                       <i class="fa-solid fa-trash"></i> Delete
                     </a>

                </div>

            </div>
        <% }) %>
    </div>

    <% } %>
</div>

</body>
</html>


