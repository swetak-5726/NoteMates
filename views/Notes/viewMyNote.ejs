<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>NoteMates</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
  <div class="container py-4">
    <div class="d-flex justify-content-between align-items-center mb-3">
      <h1><%= note.title %></h1>
      <div>
        <a href="/mynotes/edit/<%= note._id %>" class="btn btn-outline-secondary">Edit</a>
        <a href="/mynotes" class="btn btn-outline-primary">Back to notes</a>
      </div>
    </div>

    <div class="card">
      <div class="card-body">
        <p style="white-space: pre-wrap;"><%= note.content %></p>
        <hr>
        <small class="text-muted">Created: <%= new Date(note.createdAt).toLocaleString() %></small>
      </div>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
