<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>NoteMates</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/viewNote.css">
</head>
<body>
 <body>
    <%- include("../partials/navbar") %>

    <h1 class="note-title-main"><%= note.title %></h1>

    <div class="action-buttons">
        <a href="/mynotes/edit/<%= note._id %>" class="btn btn-outline-light">
            <i class="fa-solid fa-pen-to-square"></i> Edit
        </a>

        <a href="/mynotes" class="btn btn-outline-light">
            <i class="fa-solid fa-arrow-left"></i> Back
        </a>
    </div>

    <div class="view-note-card">
        <p class="note-content"><%= note.content %></p>
        <span class="note-date">Created on: <%= new Date(note.createdAt).toLocaleString() %></span>
    </div>
</body>


</body>
</html>
