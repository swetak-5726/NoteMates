<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>NoteMates</title>
</head>
<body>
    <form action="/edit/<%= note._id %>?_method=PUT" method="POST">
  <p><b>Uploaded by:</b> <%= user.username %></p>

  <label>Title:</label><br>
  <input type="text" name="title" value="<%= note.title %>" required><br><br>

  <label>Subject:</label><br>
  <input type="text" name="subject" value="<%= note.subject %>" required><br><br>

  <label>Description:</label><br>
  <textarea name="description" rows="4" cols="40"><%= note.description %></textarea><br><br>

  <button type="submit">Save Changes</button>
  <button type="button" onclick="window.location.href='/myuploads'">Cancel</button>
</form>
</body>
</html>


